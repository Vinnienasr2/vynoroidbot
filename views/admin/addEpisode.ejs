<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Episode</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5">
        <h2 class="mb-4">Add Episode</h2>
        <form action="/admin/series/<%= seriesId %>/episodes/add" method="POST">
            <div class="mb-3">
                <label for="file_id" class="form-label">File ID</label>
                <input type="text" class="form-control" id="file_id" name="file_id" required>
            </div>
            <div class="mb-3">
                <label for="poster" class="form-label">Poster URL</label>
                <input type="text" class="form-control" id="poster" name="poster" required>
            </div>
            <div class="mb-3">
                <label for="cost" class="form-label">Cost (KES)</label>
                <input type="number" class="form-control" id="cost" name="cost" required>
            </div>
            <button type="submit" class="btn btn-success">Add Episode</button>
            <a href="/admin/series/<%= seriesId %>/episodes" class="btn btn-secondary">Cancel</a>
        </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
